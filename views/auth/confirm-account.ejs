<%- include('../layouts/header') %>

<div class="py-10">
    <h1 class="text-4xl my-10 font-extrabold text-center">
        Bienes<span class="font-normal">Raíces</span>
    </h1>

    <h2 class="text-center text-2xl font-extrabold">
        <%= title %>
    </h2>

    <div class="bg-white shadow py-8 px-4 rounded-lg max-w-md mx-auto mt-4">
        <p class="<%= error ? 'bg-red-500' : 'bg-green-500' %> py-2 px-5 rounded-lg w-full font-bold text-center text-white uppercase">
            <%= message %>
        </p>
    </div>
</div>